# React + TypeScript + Vite

React Typescript component to display a Social profile with links.

## Component usage:

```js
<SocialLinkProfile
  src={src}
  name={name}
  location={location}
  description={description}
  links={links}
/>
```
# social-links-profile
